## Build & Lint

- Build: `npm run build` (runs type-check + vite build)
- Lint: `npm run lint` — always run lint after making changes
- Do NOT use `npx vue-tsc` or `npx vite` directly; use the npm scripts
